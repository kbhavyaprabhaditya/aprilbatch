version: "3.9"

# =============================================================
# Full-stack Docker Compose – Assignment
#
# Services:
#   app    – Java HTTP web server (built from source)
#   db     – MySQL 8 database
#   redis  – Redis 7 backend/cache
#
# Volumes:
#   mysql_data  – persists MySQL data across restarts
#   redis_data  – persists Redis data across restarts
#   app_logs    – persists Java app logs
# =============================================================

services:

  # -----------------------------------------------------------
  # SERVICE 1: Java Web Application
  # Built from the project's multi-stage Dockerfile.
  # Depends on db and redis being healthy before it starts.
  # -----------------------------------------------------------
  app:
    build:
      context: ..                    # repo root (where pom.xml and src/ live)
      dockerfile: docker/dockerfile
    image: bhavyaprabhaditya/assignment:latest
    container_name: java-app
    ports:
      - "9090:9090"
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: appdb
      DB_USER: appuser
      DB_PASSWORD: apppassword
      REDIS_HOST: redis
      REDIS_PORT: 6379
    volumes:
      - app_logs:/app/logs           # persist application logs
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      app-network:
        ipv4_address: 172.28.0.10  # static IP for java app
    restart: on-failure

  # -----------------------------------------------------------
  # SERVICE 2: MySQL Database
  # -----------------------------------------------------------
  db:
    image: mysql:8.0
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: appdb
      MYSQL_USER: appuser
      MYSQL_PASSWORD: apppassword
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql    # persist all database files
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-prootpassword"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      app-network:
        ipv4_address: 172.28.0.20  # static IP for mysql
    restart: unless-stopped

  # -----------------------------------------------------------
  # SERVICE 3: Redis Backend / Cache
  # -----------------------------------------------------------
  redis:
    image: redis:7-alpine
    container_name: redis-backend
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data             # persist Redis AOF/RDB snapshots
    command: redis-server --appendonly yes   # enable AOF persistence
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    networks:
      app-network:
        ipv4_address: 172.28.0.30  # static IP for redis
    restart: unless-stopped

# =============================================================
# Named Volumes
# =============================================================
volumes:
  mysql_data:
    name: assignment_mysql_data
  redis_data:
    name: assignment_redis_data
  app_logs:
    name: assignment_app_logs

# =============================================================
# Custom Bridge Network
# Subnet : 172.28.0.0/16
# Gateway: 172.28.0.1
# Static IPs assigned to each container for predictable DNS
#   app   -> 172.28.0.10
#   db    -> 172.28.0.20
#   redis -> 172.28.0.30
# =============================================================
networks:
  app-network:
    name: assignment-network
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.28.0.0/16
          gateway: 172.28.0.1
          ip_range: 172.28.0.0/24
    driver_opts:
      com.docker.network.bridge.name: assignment-br0   # custom bridge name visible in host OS
      com.docker.network.bridge.enable_icc: "true"     # inter-container communication on
      com.docker.network.bridge.enable_ip_masquerade: "true"